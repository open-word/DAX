-- Different table, blanks are not returned
EVALUATE
SUMMARIZECOLUMNS (
    Customer[CustomerType],
    'Product'[Category],
    "Sales Amount", [Sales Amount]
)
ORDER BY
    Customer[CustomerType],
    'Product'[Category]